{
  "name": "@begone/extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension: banishes unwanted elements the instant they appear — user-managed CSS selector list.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@browser-plugins/config": "workspace:*",
    "@types/chrome": "^0.2.2",
    "esbuild": "^0.28.1",
    "typescript": "^7.0.2"
  }
}
